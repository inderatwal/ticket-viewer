body {
  margin-left: auto;
  margin-right: auto;
  width: 50em;
  font: 14px "Lucida Grande", Helvetica, Arial, sans-serif;
}

#title {
 
  text-align: center;
}

/*set a fixed width to the table due to ease of use, and liquid layout is not required*/
table {
  width: 700px;
  border-collapse: collapse; 
  border-spacing: 0;
  table-layout:fixed;
}
th {
  background: rgba(235, 120, 54, 0.3);
  font-weight: bold;
  border: 1px solid #ccc; 
  height: 50px;
}


td {
  border-bottom: 1px solid rgba(235, 120, 54, 0.3);
  text-align: center;
  height: 50px;
  /*next three lines are to eliminate extra characters in a 
   subject box which could mess with the layout of table, 
   instead we restrict character input to width, and end
   with "..." */
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

table tr:hover { 
background-color: rgba(235, 109, 54, 0.5);  
}

#pagea {
  padding:10px;
  color: #0000f0;
  cursor: pointer;
  
}

#pagea:hover {
  color: #000000;
}

#links {
float:right;
padding-bottom: 10px;
padding-top: 10px;

}
#details {
 width:700px;
 border: 3px solid rgba(235, 120, 54, 0.3); 
}

#details p {
  padding-left: 10px;
  padding-right: 10px;
  font-weight: bold;
}

#details a {
 font-weight: normal;
 display:inline;
}

#backdiv{
float:right;
padding-top:10px;
}
#backbtn {
	background-color:#f7b48f;
	border:1px solid #ffffff;
	display:inline-block;
	cursor:pointer;
	color:#000000;
	font-family:Arial;
	font-size:17px;
	padding:10px 42px;
	text-decoration:none;

}

#backbtn:hover {
	background-color:#f4d5d5;
}


#sticky {
    background: #f4d5d5;
    position:fixed;
    top:0; 

}







